tempo:
  retention: 336h
  metricsGenerator:
    enabled: true
    remoteWriteUrl: "http://kube-prometheus-prometheus:9090/api/v1/write"
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: "0.0.0.0:4317"
